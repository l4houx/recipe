<div class="toast-container position-fixed bottom-0 end-0 p-3" data-controller="flash-messages">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            {{ component('toast', {message: message, type: label}) }}
        {% endfor %}
    {% endfor %}
</div>
