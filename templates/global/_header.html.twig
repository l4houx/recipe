<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home') }}">{{ website_name }}</a>
		<div class="order-lg-3 d-flex align-items-center">
            <div>
                <div class="d-flex align-items-center">
					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								Mon compte
							</a>
							<ul class="dropdown-menu">
								<li><a class="dropdown-item" href="{{ path('dashboard_main') }}">Tableau de bord</a></li>
								<li><a class="dropdown-item" href="{{ path('dashboard_main_account')}}">Mon profil</a></li>
								<li><hr class="dropdown-divider"></li>
								{% if is_granted('IS_IMPERSONATOR') %}
									<li>
										<a id="logout" class="dropdown-item" aria-current="page" href="{{ impersonation_exit_path(path('home')) }}">
											Déconnexion
										</a>
									</li>
								{% else %}
									<li>
										<a id="logout" class="dropdown-item" aria-current="page" href="{{ path('logout') }}">
											Déconnexion
										</a>
									</li>
								{% endif %}
							</ul>
						</li>
					{% else %}
						<a href="{{ path('login') }}" class="btn btn-outline-primary shadow-sm me-2 d-none d-md-block">
							S'identifier
						</a>
						<a href="{{ path('register') }}" class="btn btn-primary d-none d-md-block me-2 me-lg-0">
							S'inscrire
						</a>
					{% endif %}
                </div>
            </div>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
		</div>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link {{ app.current_route == 'home' ? 'active' : '' }}" href="{{ path('home') }}">Accueil</a>
				</li>
				{#  
				<li class="nav-item">
					<a class="nav-link {{ app.current_route starts with 'recipe_' ? 'active' : '' }}" href="{{ path('recipe_index') }}">Recettes</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{ app.current_route starts with 'post_' ? 'active' : '' }}" href="{{ path('post_index') }}">Blog</a>
				</li>
				#}
				<li class="nav-item">
					<a class="nav-link {{ app.current_route == 'contact' ? 'active' : '' }}" href="{{ path('contact') }}">
						Contact
					</a>
				</li>
			</ul>
            <form class="mt-3 mt-lg-0 ms-lg-3 d-flex align-items-center">
                <span class="position-absolute ps-3 search-icon">
                    <i class="fe fe-search"></i>
                </span>
                <label for="search" class="visually-hidden"></label>
                <input type="search" id="search" class="form-control ps-6" placeholder="Search Courses">
            </form>
		</div>
	</div>
</nav>
{% include "global/flash.html.twig" %}
