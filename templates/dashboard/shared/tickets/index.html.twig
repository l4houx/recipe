{% extends 'dashboard.html.twig' %}

{% set pagetitle = 'My tickets'|trans %}
{% block title pagetitle %}

{% block content %}
    <div class="card mb-4">
        <div class="d-lg-flex justify-content-between align-items-center card-header">
            <div class="mb-3 mb-lg-0">
                <h3 class="mb-0">{{ pagetitle }}</h3>
                <span>{{ "%resultsCount% result(s) found"|trans({'%resultsCount%': rows | length}) }}</span>
            </div>
            {% if is_granted('ROLE_VERIFIED') %}
                <div>
                    <a href="{{ path('dashboard_ticket_new') }}" class="btn btn-outline-primary btn-sm">
                        {{ 'Create'|trans }}
                    </a>
                </div>
            {% endif %}
        </div>
        {% if rows | length > 0 %}
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-12">
                        <div class="accordion accordion-flush" id="accordionTicket">
                            {% include "dashboard/shared/tickets/manage-tickets.html.twig" with {rows: rows} %}
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="card-footer">
                {% include "global/message.html.twig" with { type: "dark", message: ('No ticket found'|trans), icon: "bi bi-exclamation-circle", class: "my-2" } %}
            </div>
        {% endif %}
    </div>
{% endblock %}
