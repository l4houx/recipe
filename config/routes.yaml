controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
    prefix: "{_locale}"
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'
    trailing_slash_on_root: false
